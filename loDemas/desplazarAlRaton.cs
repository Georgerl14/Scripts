using UnityEngine;

public class desplazarAlRaton : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        if (Input.anyKey)
        {
            Debug.Log("Click izquierdo pulsado");    
        } 
    }
}
